<div id="pork">
	<div id="calling">
		<h1>John Pork</h1>
		<h2>is calling...</h2>
	</div>

	<div class="buttons">
		<div id="decline" class="button" @onclick="Decline">
			<img src="./assets/images/end-call-icon.png" />
			<p>Decline</p>
		</div>

		<div id="accept" class="button" @onclick="Accept">
			<img src="./assets/images/accept-call-icon.png" />
			<p>Accept</p>
		</div>
	</div>
</div>

<AudioHelper @ref="_audio" Src="./assets/audio/calling.mp3" Type="audio/mpeg" Loop="true" />

@code {
	[Inject]
	public IJSRuntime JS { get; set; }

	[Parameter]
	public EventCallback OnDecline { get; set; }

	[Parameter]
	public EventCallback OnAccept { get; set; }

	private AudioHelper _audio;
	private bool _playing = false;

	protected override async Task OnAfterRenderAsync(bool first)
	{
		if(first)
		{
			await Start();
		}
	}

	public async Task Start()
	{
		if (_playing)
		{
			return;
		}

		await _audio.Start();
		_playing = true;
	}

	private async Task Accept()
	{
		await _audio.Stop();
		_playing = false;
		await OnAccept.InvokeAsync();
	}

	private async Task Decline()
	{
		await _audio.Stop();
		_playing = false;
		await OnDecline.InvokeAsync();
	}
}